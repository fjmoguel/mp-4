export type Props = {
    name: string;
};

export type Album = {
    name: string;
    image?: {
    size: string;
    '#text': string;
    }[];
};

export type SimilarArtist = {
    name: string;
};

export type Tag = {
    name: string;
};  
